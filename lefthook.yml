# EXAMPLE USAGE:
#
#   Details on configuration:
#   https://github.com/evilmartians/lefthook/blob/master/docs/configuration.md
#

assert_lefthook_installed: true

pre-commit:
  scripts:
    "require-email.mjs":
      runner: node
# pre-push:
#   commands:
#     packages-audit:
#       tags: frontend security
#       run: yarn audit
#     gems-audit:
#       tags: backend security
#       run: bundle audit
#
# pre-push:
#   # parallel: true
#   commands:
#     format:
#       # glob: "*.{js,ts,jsx,tsx}"
#       files: git diff --name-only origin/main
#       run: "pnpm biome check --linter-enabled=false --apply --no-errors-on-unmatched {files} && git update-index --again"

# rubocop:
#       tags: backend style
#       glob: "*.rb"
#       exclude: '(^|/)(application|routes)\.rb$'
#       run: bundle exec rubocop --force-exclusion {all_files}
#     govet:
#       tags: backend style
#       files: git ls-files -m
#       glob: "*.go"
#       run: go vet {files}
#   scripts:
#     "hello.js":
#       runner: node
#     "any.go":
#       runner: go run

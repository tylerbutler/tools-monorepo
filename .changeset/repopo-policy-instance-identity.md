---
"repopo": minor
---

Distinguish duplicate configured policy instances in exclusions, diagnostics, results, and performance metrics.

Duplicate instances must now set a unique `instanceId` in the `policy()` options.

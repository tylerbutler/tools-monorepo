[build]
  # This will ignore changes outside of the packages/repopo and packages/repopo-docs directories,
  # which means the builds will only be triggered by changes to the repopo and repopo-docs packages.
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF packages/repopo packages/repopo-docs"

[package]
name = "tree-sitter-ccl"
description = "ccl grammar for the tree-sitter parsing library"
version = "0.1.0"
keywords = ["incremental", "parsing", "ccl"]
categories = ["parsing", "text-editors"]
repository = "https://github.com/tylerbutler/tools-monorepo"
edition = "2018"
license = "MIT"

build = "bindings/rust/build.rs"
include = [
  "bindings/rust/*",
  "grammar.js",
  "queries/*",
  "src/*",
]

[lib]
path = "bindings/rust/lib.rs"

[dependencies]
tree-sitter = "~0.25.10"

[build-dependencies]
cc = "1.0"

================================================================================
Basic key-value pairs
================================================================================

name = John Doe
age = 30
active = true

--------------------------------------------------------------------------------

(document
  (entry
    (single_line_key)
    (assignment)
    (single_line_value))
  (entry
    (single_line_key)
    (assignment)
    (single_line_value))
  (entry
    (single_line_key)
    (assignment)
    (single_line_value)))

================================================================================
Empty key (list syntax)
================================================================================

= item1
= item2
= item3

--------------------------------------------------------------------------------

(document
  (entry
    (assignment)
    (single_line_value))
  (entry
    (assignment)
    (single_line_value))
  (entry
    (assignment)
    (single_line_value)))

================================================================================
Empty value
================================================================================

empty_key =

--------------------------------------------------------------------------------

(document
  (entry
    (single_line_key)
    (assignment)
    (single_line_value)))

================================================================================
Key and value only
================================================================================

key_only =
= value_only

--------------------------------------------------------------------------------

(document
  (entry
    (single_line_key)
    (assignment)
    (single_line_value))
  (entry
    (assignment)
    (single_line_value)))
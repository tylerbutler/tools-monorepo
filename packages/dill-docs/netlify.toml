[build]
  # This will ignore changes outside of the packages/dill and packages/dill-docs directories,
  # which means the builds will only be triggered by changes to the dill and dill-docs packages.
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF packages/dill packages/dill-docs"

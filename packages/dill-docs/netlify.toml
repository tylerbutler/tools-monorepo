[build]
  # Only trigger builds when files in dill-docs or dill-cli change.
  # Paths are relative to repo root (Netlify runs from /opt/build/repo).
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- packages/dill-docs/ packages/dill-cli/"

[build]
  # Only trigger builds when files in dill-docs or dill-cli change.
  # The command runs from the base directory but git diff paths are relative to repo root.
  ignore = "cd ../.. && git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- packages/dill-docs/ packages/dill-cli/"

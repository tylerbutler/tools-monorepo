[build]
  # This will ignore changes outside of the packages/dill-cli and packages/dill-docs directories,
  # which means the builds will only be triggered by changes to the dill-cli and dill-docs packages.
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF packages/dill-cli packages/dill-docs"

[build]
  # This will ignore changes outside of the packages/ccl-docs directory,
  # which means the builds will only be triggered by changes to the ccl-docs package.
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF packages/ccl-docs"